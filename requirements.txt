# --- app / UI ---
streamlit>=1.42
plotly>=5.20                 # interactive charts in the views
# --- data + analysis ---
yfinance>=0.2.40
pandas>=2.0
numpy>=1.24
scipy>=1.11
requests>=2.31               # mfapi.in / Kuvera fetches (mf_data.py) + deep_checks
beautifulsoup4>=4.12         # screener.in shareholding/pledge scrape (deep_checks)
mplfinance>=0.12.10b0
pyarrow>=14.0
lxml>=4.9                    # yfinance earnings_dates (PEAD announcement history) needs it
