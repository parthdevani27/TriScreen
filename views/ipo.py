"""IPO section — placeholder (planned)."""

from __future__ import annotations

import streamlit as st

st.markdown('<p class="app-title">🚀 IPO Screener</p>', unsafe_allow_html=True)
st.markdown('<p class="app-sub">Coming soon — same honest, evidence-first approach.</p>',
            unsafe_allow_html=True)

st.info("This section is planned. It will screen mainboard & SME IPOs from free "
        "NSE/BSE data — with the same split of *computable* signals vs *manual-review* "
        "items (anchor-investor quality, promoter/selling-shareholder intent, valuation "
        "vs listed peers, GMP caveats).")

st.markdown('<div class="disclaimer">Planned free sources: NSE/BSE IPO pages, SEBI '
            'RHP/DRHP filings, exchange bid/subscription data. Nothing here yet — '
            'not financial advice.</div>', unsafe_allow_html=True)
